# Changelog

## 未发布

### 重大变更

- 练习模式收敛为背八股、模拟面试、场景题和刷力扣，不再提供独立简历出题模式。
- 配置改为按模式区分：背八股与场景题只配置主题；模拟面试配置简历、面试官风格、是否手撕代码和难度；刷力扣必须配置编程语言。
- 删除统一题数、追问策略和所有隐式默认配置。

### 新增

- 新增常驻本地练习工作台，直接访问进行中练习、练习档案和热题 100，无需先由 Agent 调用展示工具。
- 新增刷力扣模式，内置力扣热题 100 官方分组快照；随机抽题不依赖模型生成，支持原题链接、难度、全局完成状态和按题型题目列表。
- 新增 `interview_get_leetcode_catalog` 与 `interview_set_leetcode_completion` 原子工具，聊天工具和可视化 UI 共用同一题库与进度。
- 新增“继续练习”原子工具与 UI 命令，按权威状态恢复当前题、评价、讲解、下一题或总结，已结束练习必须明确确认后才能重新打开。
- 题目卡支持直接看答案，生成详细讲解和“直接背”，不会伪造作答与评分。
- 结束非力扣练习时基于完整历史生成总体总结、表现亮点和改进建议，并持久化展示。
- 补全练习与题目的增删改查工具、应用用例和可视化编辑入口。
- 切换练习时向模型返回配置、总结、全部题目、历次作答、评价和讲解。

### 改进

- 工作台移除重复的当前练习视图；“进行中”仅显示未结束练习，“练习档案”仅显示已结束练习，当前选择统一由题目时间轴表达。
- 会话与练习改为双向唯一绑定；新会话只有在新建、切换或重新打开练习后才显示时间轴，后切换的会话会接管完整练习进度，结束练习后自动解绑。
- 移除共享的 `global` 会话兜底；全新数据库结构直接约束一个练习只能对应一个会话游标，不提供旧数据迁移。
- 统一界面字体层级，仅主标题使用粗体，移除标题周围无业务意义的眉题和副标题。
- 将业务事实 `events` 与 Agent 任务 `agentTasks` 分离；只有内容生成或正式会话卡片展示才会唤醒 Agent。
- 切换练习和重新作答使用本地刷新与 Toast 反馈；力扣抽题完成后触发对话，由 Agent 在会话最新位置展示正式题目卡片。
- 刷力扣当前题、题目列表、练习档案、时间轴和 Markdown 导出统一展示固定题库元数据；UI 新建和聊天工具抽题均能打开同一题目卡。
- 结束力扣练习改为纯本地完成，只归档本次抽取的题目清单，不再触发模型生成表现分析。
- 力扣当前题卡移除题目列表入口，改为生成或展开独立的算法讲解；讲解包含解法推导、正确性、边界与复杂度，并且只生成练习配置语言的一份完整答案代码。
- 题目时间轴仅保留节点与连线；点击节点默认打开题目，并通过题目、作答记录、答案三个标签查看详情，其中每次作答集中展示回答、评分和点评。
- 点评讲解卡片改为上下结构，移除作答展示和“直接背”复制按钮，并统一使用“点评讲解”命名。
- UI 切换练习后立即通知 Agent 加载完整上下文，并由 Agent 返回唯一的切换确认文本。
- 一次只生成一道简单扼要的问题，练习题数由用户主动结束控制。
- 新建练习表单不预选模式、难度或布尔配置，所有值必须由用户明确选择。
- 清理可视化界面的装饰性英文标签和无业务价值提示。

## 0.3.0

### 重大变更

- 将四个多命令工具替换为具备硬 Schema 的原子工具，不再保留旧工具名和 `command` 调用方式。
- 新增统一 `InterviewCoordinator`，Agent 与 UI 入口共享动作执行、事件派发和交互结果生成。
- 工具结果切换为 `dsh-interview/interaction-v1`，Client 不再解析旧的资源文本格式。
- 回答后的流程改为自动完成本题复盘，移除显式请求讲解工具和半成品评价卡片。

### 修复

- `interview_present_question.prompt` 现在是 `required + minLength: 1`，空题目调用在执行前被拒绝。
- Agent 参数和工作流错误被标记为可恢复内部错误，不再生成用户可见错误卡片。
- Assistant Text 由结构化响应契约控制，只做状态确认，不再复述 UI 内容。
- 展示协议使用应用层提供的强类型资源引用，避免把题目 ID 误作练习 ID 查询。
- “直接背”改为非空必填字段，复盘卡片始终同时包含评价、参考讲解和背诵要点。

### 改进

- Client 通过练习、题目和作答 ID 查询权威读模型，历史题目、评价和讲解卡片不依赖工具参数。
- 原子工具使用 `additionalProperties: false` 和字段级说明，降低模型误用概率。
- 聊天流使用统一复盘卡片展示题目、本次作答、评价、讲解和“直接背”，并提供下一题、重答和结束操作。
- 可视化界面重构为题目卡、双栏评分复盘卡和练习历史表格，统一提供复制、切换、删除和导出操作。
- 练习配置改为五项显式必填，Agent 不得推断或采用默认值；出题严格限制为每轮一道简单扼要的问题。

## 0.2.0

### 重大变更

- 使用面试领域模型和显式状态机重写全部业务流程，不兼容 0.1.x 的工具协议与 JSON 数据。
- 将单一 `interview` 工具拆分为 `interview_session`、`interview_question`、`interview_answer` 和 `interview_library`。
- 将八股模式的协议标识统一为 `bagu`。
- 数据存储从单文件 JSON 切换为启用事务和外键约束的 SQLite。

### 新增

- 自然语言与可视化 UI 共享应用用例和权威读模型。
- 新增练习档案、搜索筛选、历次作答对比、能力复盘和题目时间轴。
- 新增受控 Markdown 下载令牌，避免通过 HTTP 暴露任意本地文件路径。
- 新增领域、应用、适配器、存储、Client 和端到端自动化测试。

### 改进

- Client 改为模块化源码并由 esbuild 生成 DSH 可加载产物。
- UI 使用明确阶段提示和十格掌握度轨道，并支持窄屏与键盘焦点。
- 错误响应提供稳定错误码，HTTP 响应默认禁止缓存。

## 0.1.0

- Initial open source release.
